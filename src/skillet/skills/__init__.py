# This package only exists so skillet's own `[project.entry-points."skillet.skills"]`
# entry point resolves to a real on-disk directory. The actual skill files are
# the SKILL.md (and any helpers) in the subdirectories next to this module.
